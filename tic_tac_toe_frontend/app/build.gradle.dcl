androidApplication {
    namespace = "org.example.app"

    dependencies {
        // No external libs needed for this simple app
        // Keep sample modules off the app to avoid unused code paths
    }
}
